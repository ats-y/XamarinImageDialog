<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Dialog.Views.MainPage"
    Title="Trial Dialog">
    <StackLayout>

        <Button
            x:Name="ShowDialogButton"
            Command="{Binding ShowCostomDialogCommand}"
            Text="カスタムダイアログ"/>

        <Button
            Text="照合ダイアログ（全部）"
            Command="{Binding ShowCollationCommand}"/>

        <Button
            Text="照合ダイアログ（エラーなし）"
            Command="{Binding ShowNonErrorCommand}"/>
    </StackLayout>
</ContentPage>